# Public frontend configuration only.
# Never place MIDTRANS_SERVER_KEY, DATABASE_URL, or other server secrets here.
VITE_API_BASE_URL="http://localhost:8787"

# Optional application URL used by the frontend for canonical/self links.
VITE_APP_URL="http://localhost:5173"

# Legacy AI Studio variables are intentionally not required by the payment flow.
# If Gemini features are added later, route secret-bearing API calls through the backend.
