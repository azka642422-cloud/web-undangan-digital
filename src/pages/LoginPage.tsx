import React,{useState}from'react';
import{loginUser}from'../services/auth';
export function LoginPage({onNavigate}:{onNavigate:(to:string)=>void}){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError('');setBusy(true);try{const user=await loginUser(email,password);onNavigate(user.role==='ADMIN'?'/admin':'/dashboard');}catch{setError('Email atau kata sandi tidak valid.');}finally{setBusy(false)}}return <div className="min-h-[70vh] flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl border border-stone-200 bg-white p-7 shadow-sm"><h1 className="text-2xl font-semibold">Masuk</h1><p className="mt-2 text-sm text-stone-600">Akses dashboard AKSARA UNDANGAN.</p><label className="mt-6 block text-sm font-medium">Email<input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-stone-300 px-4 py-3"/></label><label className="mt-4 block text-sm font-medium">Kata sandi<input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-stone-300 px-4 py-3"/></label>{error&&<p role="alert" className="mt-4 text-sm text-red-700">{error}</p>}<button disabled={busy} className="mt-6 w-full rounded-xl bg-stone-900 px-5 py-3 font-semibold text-white disabled:opacity-60">{busy?'Memeriksa...':'Masuk'}</button></form></div>}
