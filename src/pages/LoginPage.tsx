import React,{useState}from'react';import{loginUser,registerUser}from'../services/auth';export function LoginPage({onNavigate,returnTo='/dashboard'}:{onNavigate:(to:string)=>void;returnTo?:string}){const[mode,setMode]=useState<'login'|'register'>('login'),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError('');setBusy(true);try{const u=mode==='login'?await loginUser(email,password):await registerUser(name,email,password);onNavigate(u.role==='ADMIN'?'/admin':returnTo)}catch(e){const c=e instanceof Error?e.message:'';setError(c==='ACCOUNT_EXISTS'?'Email sudah terdaftar. Silakan masuk.':mode==='register'?'Pendaftaran gagal. Pastikan password minimal 10 karakter, memiliki huruf besar, huruf kecil, dan angka.':'Email atau kata sandi tidak valid.')}finally{setBusy(false)}}return <div className="min-h-[70vh] flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl border border-stone-200 bg-white p-7 shadow-sm"><h1 className="text-2xl font-semibold">{mode==='login'?'Masuk':'Buat Akun'}</h1><p className="mt-2 text-sm text-stone-600">AKSARA UNDANGAN — akun customer untuk checkout dan pengelolaan undangan.</p>{mode==='register'&&<label className="mt-6 block text-sm font-medium">Nama<input required autoComplete="name" value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3"/></label>}<label className="mt-4 block text-sm font-medium">Email<input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3"/></label><label className="mt-4 block text-sm font-medium">Kata sandi<input required type="password" autoComplete={mode==='login'?'current-password':'new-password'} value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border px-4 py-3"/></label>{error&&<p role="alert" className="mt-4 text-sm text-red-700">{error}</p>}<button disabled={busy} className="mt-6 w-full rounded-xl bg-stone-900 px-5 py-3 font-semibold text-white disabled:opacity-60">{busy?'Memproses...':mode==='login'?'Masuk':'Daftar & Lanjutkan'}</button><button type="button" onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}} className="mt-4 w-full text-sm underline">{mode==='login'?'Belum punya akun? Daftar':'Sudah punya akun? Masuk'}</button></form></div>}
