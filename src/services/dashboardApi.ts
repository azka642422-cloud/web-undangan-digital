const API_BASE=(import.meta.env.VITE_API_BASE_URL||'').replace(/\/$/,'');async function req(path:string,init?:RequestInit){const r=await fetch(`${API_BASE}${path}`,{...init,credentials:'include',headers:{Accept:'application/json',...(init?.body?{'Content-Type':'application/json'}:{}),...init?.headers}});if(!r.ok)throw new Error(r.status===401?'UNAUTHENTICATED':r.status===403?'FORBIDDEN':'REQUEST_FAILED');return r.json()}export type CustomerItem={order_number:string;package_slug:string;template_slug:string;gross_amount:number;status:string;created_at:string;slug:string;is_published:boolean;active_until:string|null};export type AdminOrder=CustomerItem&{customer_name:string;customer_email:string};export type PendingMessage={id:string;sender_name:string;relationship:string|null;attendance:string;message:string;created_at:string;slug:string};export type AdminRsvp={id:string;guest_name:string;guest_phone:string|null;attendance:string;guest_count:number;notes:string|null;created_at:string;slug:string};export const getCustomerDashboard=async():Promise<CustomerItem[]> =>(await req('/api/dashboard')).items||[];export const getAdminDashboard=async()=>(await req('/api/admin/dashboard')) as{orders:AdminOrder[];stats:{orders:number;paid_gross:number};pendingMessages:PendingMessage[];rsvps:AdminRsvp[]};export const moderateMessage=async(id:string,approved:boolean)=>req(`/api/admin/messages/${encodeURIComponent(id)}/moderate`,{method:'POST',body:JSON.stringify({approved})});
