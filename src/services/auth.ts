export type SessionUser={id:string;email:string;displayName:string;role:'CUSTOMER'|'ADMIN'};const API_BASE=(import.meta.env.VITE_API_BASE_URL||'').replace(/\/$/,'');async function request(path:string,init?:RequestInit){const r=await fetch(`${API_BASE}${path}`,{...init,credentials:'include',headers:{Accept:'application/json',...(init?.body?{'Content-Type':'application/json'}:{}),...init?.headers}}),d=await r.json().catch(()=>null);if(!r.ok)throw new Error(d?.code||'REQUEST_FAILED');return d}export async function getSession():Promise<SessionUser|null>{try{return(await request('/api/auth/session')).user}catch{return null}}export async function loginUser(email:string,password:string):Promise<SessionUser>{return(await request('/api/auth/login',{method:'POST',body:JSON.stringify({email,password})})).user}export async function registerUser(displayName:string,email:string,password:string):Promise<SessionUser>{return(await request('/api/auth/register',{method:'POST',body:JSON.stringify({displayName,email,password})})).user}export async function logoutUser(){await request('/api/auth/logout',{method:'POST'})}
